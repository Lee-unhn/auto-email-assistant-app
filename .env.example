# Optional: pre-seed API keys for dev. In the packaged app, keys are entered in
# Settings and stored ENCRYPTED via Electron safeStorage (never plaintext, never logged).
# Copy to .env for local dev only. .env is gitignored.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
